# Only the corpus tooling reads these. The extension itself keeps the key in extension
# storage, pasted into the popup, and never in a file.

# Required by: npm run record
TYPESAFE_API_KEY=

# Optional. Point record at a local gateway shim instead of https://api.typesafe.ai
JEV_BASE_URL=
